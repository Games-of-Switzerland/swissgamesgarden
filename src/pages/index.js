import React from 'react'
import Layout from '../containers/Layout';
import Results from "../components/Results";

const Home = () => {
  return (
    <Layout>
      <Results/>
    </Layout>
  );
};

export default Home;
